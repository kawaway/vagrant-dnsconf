require 'spec_helper'

describe Vagrant::Dns::Conf do
  it 'has a version number' do
    expect(Vagrant::Dns::Conf::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
